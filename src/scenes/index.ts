import { StartScene } from './start.scene';
import { HelpScene } from './help.scene';
import { StartedScene } from './started.scene';
import { CompletedScene } from './completed.scene';
import { StopScene } from './stop.scene';

export const scenes = [
  StartScene,
  HelpScene,
  StartedScene,
  CompletedScene,
  StopScene,
];
